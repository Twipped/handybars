
import {
	log,
	if as _if,
	not,
	has,
	is,
	isNot,
	all,
	any,
	with as _with,
	each,
	get,
} from './kit';

const helpers = {
	if:     _if,
	with:   _with,
	each,
	log,
	not,
	unless: not,
	has,
	is,
	isNot,
	all,
	any,
	get,
	lookup: get,
};

export default helpers;
